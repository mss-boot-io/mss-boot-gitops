apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: store
    version: v1
  name: store
spec:
  selector:
    matchLabels:
      app: store
      version: v1
  template:
    metadata:
      annotations:
        prometheus.io/path: /metrics
        prometheus.io/port: "5000"
        prometheus.io/scrape: "true"
      labels:
        app: store
        version: v1
    spec:
      containers:
        - env:
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: STAGE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          image: mssbootio/store:f12a7d0e02dbcdf8e03d2967689366bc6cba221c
          name: store
          ports:
            - containerPort: 9090
          resources: {}
          volumeMounts: []
      volumes: []
