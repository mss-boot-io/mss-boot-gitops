apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: store
    version: v1
  name: store
spec:
  selector:
    matchLabels:
      app: store
      version: v1
  template:
    metadata:
      annotations:
        prometheus.io/path: /metrics
        prometheus.io/port: "5000"
        prometheus.io/scrape: "true"
      labels:
        app: store
        version: v1
    spec:
      containers:
        - env:
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: STAGE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          image: mssbootio/store:68b34f0b9a23afa7b0deac3dbc1aafbe2eaee79b
          name: store
          ports:
            - containerPort: 9090
          resources: {}
          volumeMounts: []
      volumes: []
